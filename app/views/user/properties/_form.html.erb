<%= form_for(@user_property) do |f| %>
  <% if @user_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_property.errors.count, "error") %> prohibited this user_property from being saved:</h2>

      <ul>
      <% @user_property.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
